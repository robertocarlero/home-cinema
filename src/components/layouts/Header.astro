---
/**
 * Header component for the Home Cinema application.
 * Displays navigation bar with logo, menu items, and call-to-action button.
 */

import { Menu } from 'lucide-react';

import logo from '@/assets/logo.png';
import WhatsAppIcon from '@/components/icons/WhatsAppIcon.astro';
import { Button } from '@/components/ui/button';
import {
	NAVIGATION_ROUTES,
	HOME_ROUTE,
	WHATSAPP_ROUTE,
} from '@/constants/navigation';

export interface Props {
	className?: string;
}

const { className = '' } = Astro.props;
---

<header class={`fixed top-0 z-50 bg-black/50 w-full ${className}`}>
	<div class="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8">
		<div class="flex items-center justify-between h-16">
			<!-- Logo Section -->
			<a class="flex items-center space-x-3" href={HOME_ROUTE}>
				<!-- Logo Image -->
				<img
					alt="Home Cinema Logo"
					class="h-8 object-contain"
					src={logo.src}
				/>
			</a>

			<!-- Navigation Menu -->
			<nav class="hidden md:flex items-center space-x-8">
				{
					NAVIGATION_ROUTES.map((route) => (
						<a
							class="text-white text-sm font-medium hover:text-neutral-200 transition-colors"
							href={route.href}
						>
							{route.label}
						</a>
					))
				}
			</nav>

			<!-- Right Section -->
			<div class="flex items-center space-x-4">
				<!-- WhatsApp Section -->
				<a
					href={WHATSAPP_ROUTE}
					rel="noopener noreferrer"
					target="_blank"
				>
					<Button variant="ghost">
						<WhatsAppIcon size={20} />
						WhatsApp
					</Button>
				</a>

				<!-- CTA Button -->
				<Button>Get Free Consultation</Button>
			</div>

			<!-- Mobile Menu Button -->
			<Button
				aria-label="Toggle menu"
				className="md:hidden text-white hover:bg-gray-800"
				size="icon-sm"
				variant="ghost"
			>
				<Menu className="w-6 h-6" />
			</Button>
		</div>
	</div>
</header>
